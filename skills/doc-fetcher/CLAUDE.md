## Skills and rules
- When a library's API is uncertain, generate a `<lib>-docs` skill with `doc-fetcher` and read the fetched docs instead of guessing. Generated caches live under `.claude/skills/*/cache/` and stay out of git.
