## Workflow
- Design specs section by section with an approval checkpoint after each (`iterative-spec-design`). Never write the whole spec and then ask for feedback — a wrong assumption in section 1 invalidates everything after it.
