---
name: Demo
description: Demo bundle used by the CLI registry client tests.
tags: [demo, fixture]
author: Fixture
suggests: [second]
---

# Demo bundle

This bundle exists to exercise `download()` — unzipping, stripping the
leading `demo/` root, and dropping directory entries.
